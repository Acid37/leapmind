# M6 v1.0 无状态画像引擎 — 当前状态与待办

> 日期：2026-08-08 | 分支：`feat/user-profile-zhangweitao-clean`
> PR 地址：https://github.com/dgut-hsd/leapmind/pull/new/feat/user-profile-zhangweitao-clean

## 一、已完成 ✅

### 核心功能
| 功能 | 说明 | 验证 |
|------|------|:--:|
| Python 无状态画像引擎 | v1.0 契约，10 种事件类型，三态响应 | 221 测试通过 |
| Java→Python HTTP 适配器 | `HttpProfileEngineAdapter`，RestTemplate 调用 | 日志确认已连接 |
| AI 摘要 | DeepSeek API 接入，一键生成中文诊断分析 | 实测置信度 0.929 |
| 知识点名称显示 | Java API 返回 `kpName`，前端知识树展示真实名称 | 勾股定理逆定理/勾股数/勾股定理应用 |
| 学习轨迹 | 前端调用 Python timeline 接口，中文标签映射 | 5 条真实事件 |
| 前后端联调 | 前端 ↔ Java ↔ Python ↔ MySQL 全链路 | 9 项测试通过 |

### Bug 修复
| 问题 | 修复 |
|------|------|
| Spring Security formLogin 拦截 JSON 登录 | `SecurityConfig` 添加 `.formLogin(disable)` |
| knowledge-status kpId 必填导致前端报错 | `@RequestParam(required=false)` + service 返回全部 |
| `.env` 未加载导致 OpenAI 配置无效 | `main.py` 启动时 `load_dotenv()` |
| 前端 key 不匹配导致永远显示 demo 数据 | 添加 `knowledge`/`summaryProfile`/`events`/`kpName` 映射 |
| Admin 包名大小写不一致导致启动失败 | 8 个文件统一为大写 |
| 认证中间件拦截 `/api/internal/` | 添加白名单 |
| 密码硬编码在源码中 | 改用环境变量注入 |

### 测试环境
| 组件 | 状态 |
|------|:--:|
| Python 服务 (8001) | ✅ |
| Java 服务 (8080) | ✅ |
| 前端 (5174) | ✅ |
| MySQL `leapmind` | ✅ |
| 测试用户 23 | `m6_apifox_20260805` / `M6Demo-9f4K2!` |

### 文档
- `docs/notifications/M6-v1-联调测试手册.md`
- `docs/notifications/M6-v1-联调测试结果.md`
- `docs/notifications/M6-v1-规格审查报告.md`
- `docs/notifications/M6-v1-回复-M2组.md`

---

## 二、未完成 ❌

### P0 — 阻塞项
| 事项 | 负责人 | 说明 |
|------|:--:|------|
| **事件→引擎自动触发** | 吴铭熹（Java） | record-event 写入后需自动调 Python 引擎并回写画像 |
| M3 接入事件上报 | 刘金华 | `weak_point_changed` 事件 |
| M1 确认 isCorrect 为布尔值 | 刘楷钦 | JSON `true`/`false`，不能是 `1`/`0` |

### P1 — 重要
| 事项 | 负责人 | 说明 |
|------|:--:|------|
| M2 接入事件上报 | 阳睿 | `request_explanation` + `explanation_feedback` |
| M6 接入事件上报 | 张梓鸿 | `mark_reviewed` + `preference_changed` |
| M7 确认 topic 隐私合规 | 周贺浚 | topic 限制 1-120 码点 |

### P2 — 后续
| 事项 | 负责人 | 说明 |
|------|:--:|------|
| C4 补充 2 行测试覆盖 | 张伟涛 | `profile_engine.py` 第 270/687 行 |
| M4 接入事件上报 | 王成媛 | `lecture_interact` 事件 |
| 前端统计卡片/雷达图 | 不在 M6 范围 | 需产品确认归属 |
| 真实 OpenAI 集成验证 | 张伟涛 | 当前 DeepSeek 可用，需补 OpenAI 测试 |

### P3 — 算法迭代
| 事项 | 说明 |
|------|------|
| 教师问卷权重升级 | 当前临时权重 40/25/20/15 |
| 错题本事件接入 | 25% 错题本权重暂不参与 |
| 大模型增强困惑/关注/节奏 | 当前为基础算法 |
| 混淆矩阵验证 | 与教师标注数据对比 |

---

## 三、架构全景

```
┌─────────────────────────────────────────────────────────┐
│ 前端 (React :5174)                                       │
│ 学习画像页 / 知识树 / 学习轨迹 / 复习提醒                   │
└──────────┬──────────────────────────────────────────────┘
           │ Vite proxy
           ▼
┌─────────────────────────────────────────────────────────┐
│ Java (:8080)                                             │
│                                                          │
│ M6ContextController                                      │
│   POST /record-event    ← M1~M7 各模块上报事件            │
│   GET  /{userId}        → 画像查询                       │
│   GET  /knowledge-status → 知识点状态                     │
│   GET  /summary          → 场景化摘要                     │
│                                                          │
│ HttpProfileEngineAdapter ──→ Python 引擎                  │
│   🔴 当前未被调用（缺触发逻辑）                              │
└──────────┬──────────────────────────────────────────────┘
           │ 内部 HTTP
           ▼
┌─────────────────────────────────────────────────────────┐
│ Python (:8001)                                           │
│                                                          │
│ POST /api/internal/ai/build-profile  ← 无状态画像引擎      │
│ GET  /api/user-profile/{id}/knowledge-status              │
│ GET  /api/user-profile/{id}/timeline                     │
│                                                          │
│ ✅ 引擎就绪    ✅ AI摘要    ✅ DeepSeek 接入               │
└──────────┬──────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────────────────┐
│ MySQL (leapmind)                                         │
│ users / user_events / user_profiles / user_knowledge_mastery │
│ event_collections / knowledge_points / review_schedules  │
└─────────────────────────────────────────────────────────┘
```

---

## 四、下一步行动

1. **创建 PR**：`feat/user-profile-zhangweitao-clean` → `develop`
2. **协调吴铭熹**：实现事件触发引擎的定时任务
3. **跟进各组接入**：M1/M2/M3/M6/M7 事件上报
4. **环境变量说明**：通知各组在本地 `.env` 中配置 `MYSQL_PASSWORD`
