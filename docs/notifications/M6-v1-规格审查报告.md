# M6 v1.0 规格审查报告（C5 收尾）

> **日期**：2026-08-06 | **审查对象**：`src/landppt/m6/schemas.py`（470 行）
> **对照基准**：`docs/M6-v1-接口变更通知清单.md`
> **测试状态**：221 项全部通过 | schemas.py 覆盖率 99%

---

## 审查结论：✅ 通过

schemas.py 与接口变更通知清单中的 v1.0 契约定义完全一致，未发现契约漏洞或不一致项。

---

## 逐维度审查

### 1. 事件类型覆盖 ✅

| 事件类型 | 来源模块 | schemas.py 定义 | 通知清单 | 状态 |
|---------|:----:|------|------|:----:|
| `answer_question` | M1 | `AnswerQuestionData` (line 99) | §3.1 | ✅ |
| `finish_practice` | M1 | `FinishPracticeData` (line 117) | §3.1 | ✅ |
| `request_explanation` | M2 | `RequestExplanationData` (line 125) | §3.2 | ✅ |
| `explanation_feedback` | M2 | `ExplanationFeedbackData` (line 138) | §3.2 | ✅ |
| `weak_point_changed` | M3 | `WeakPointChangedData` (line 146) | §3.3 | ✅ |
| `lecture_interact` | M4 | `LectureInteractData` (line 154) | §3.4 | ✅ |
| `lesson_material_used` | M5 | `LessonMaterialUsedData` (line 162) | §3.5 | ✅ |
| `ask_doubt` | M7 | `AskDoubtData` (line 170) | §3.7 | ✅ |
| `mark_reviewed` | M6 | `MarkReviewedData` (line 186) | §3.6 | ✅ |
| `preference_changed` | M6 | `PreferenceChangedData` (line 200) | §3.6 | ✅ |

`EVENT_SOURCES` 映射表（line 44-55）与 `EVENT_DATA_MODELS` 注册表（line 219-230）完全匹配。

### 2. 字段约束 ✅

| 约束项 | 通知清单要求 | schemas.py 实现 | 状态 |
|-------|------|------|:----:|
| `isCorrect` 严格 bool | JSON `true`/`false`，拒绝 0/1 | `Field(strict=True)` (line 102) | ✅ |
| `Identifier` 正则 | `[A-Za-z0-9][A-Za-z0-9._:-]{0,63}` | 同上 (line 31) | ✅ |
| `StrictFraction` | 0-1 范围，拒绝越界 | `Field(strict=True, ge=0, le=1)` (line 32) | ✅ |
| 时区强制 | `occurredAt` 必须含 UTC 偏移 | `requireTimezone()` (line 246) | ✅ |
| 显式 null 拒绝 | 可选字段可省略但不得为 null | `rejectNullOptionalFields` (line 266) | ✅ |
| 未知字段拒绝 | 不传未定义字段 | `extra="forbid"` (line 96) | ✅ |
| topic 长度校验 | 去空白后 1-120 码点 | `validateTopic` (line 177) | ✅ |

### 3. 三态响应 ✅

| 状态 | 触发条件 | schemas.py 类 | 通知清单 |
|------|------|------|:----:|
| `READY` | ≥5 条有效事件 | `ReadyResponse` (line 424) | ✅ |
| `INSUFFICIENT_DATA` | 1-4 条有效事件 | `InsufficientDataResponse` (line 439) | ✅ |
| `NO_CHANGE` | 0 条有效事件 | `NoChangeResponse` (line 453) | ✅ |

联合判别器：`BuildProfileResponse = Annotated[ReadyResponse | InsufficientDataResponse | NoChangeResponse, Field(discriminator="status")]` (line 466)

### 4. 拒绝条件 ✅

| HTTP | 触发条件 | schemas.py 校验 |
|:----:|------|------|
| 400 | 契约版本 ≠ `"1.0"` | `contractVersion: Literal[CONTRACT_VERSION]` (line 303) |
| 400 | sourceModule 与 eventType 不匹配 | `validateEventContract` (line 282) |
| 400 | 时间不含时区 | `requireTimezone` (line 279) |
| 400 | dbEventId 不严格递增 | `validateEventWindow` (line 312) |
| 400 | data 含未知字段 | `extra="forbid"` (line 96) |
| 400 | 可选字段显式 null | `rejectNullOptionalFields` (line 266) |
| 400 | kpId 对 answer_question 必填 | (line 284) |
| 400 | 事件数超过 1000 | `max_length=MAX_EVENTS_PER_REQUEST` (line 310) |

### 5. 过渡兼容 ✅

`CalculateAllResponse`、`EventProcessResponse`、`ProfileData` 已从代码库完全清除。

### 6. 路由一致性 ✅

| 路由 | schemas.py 模型 | router.py |
|------|------|:----:|
| `POST /api/internal/ai/build-profile` | `BuildProfileResponse` (line 466) | `response_model=BuildProfileResponse` ✅ |
| `GET /api/user-profile/{userId}/knowledge-status` | `KnowledgeStatusResponse` (line 69) | `response_model=KnowledgeStatusResponse` ✅ |
| `GET /api/user-profile/{userId}/timeline` | `TimelineResponse` (line 86) | `response_model=TimelineResponse` ✅ |
| 旧 POST 路由 `/api/review/calculate-all` | — | 返回 404 ✅ |
| 旧 POST 路由 `/api/events/process` | — | 返回 404 ✅ |

---

## 覆盖率缺口分析（3 行未覆盖，均为非关键边缘路径）

| 行号 | 代码 | 性质 |
|:----:|------|------|
| 281 | `if not self.eventId.strip()` | 空 eventId 校验 — 测试中无空白 eventId |
| 435 | `raise ValueError("READY 的...")` | ReadyResponse 版本号不匹配错误路径 |
| 449 | `raise ValueError("INSUFFICIENT_DATA 的...")` | InsufficientDataResponse 版本号错误路径 |

**评估**：这 3 行都是错误路径中的 raise 语句，正常场景不会被触发。不影响引擎核心逻辑的正确性验证。99% 覆盖率已充分。

---

## profile_engine.py 未覆盖行分析（12 行，均为环境依赖或防御性代码）

| 行号 | 代码 | 原因 |
|:----:|------|------|
| 188-190 | `from openai import AsyncOpenAI` | 仅当真实 OpenAI SDK 可用时触发 |
| 233 | `return OpenAISummaryClient(...)` | 仅当三项 OpenAI 环境变量全部配置时触发 |
| 375 | `combined = localSummary` | 边缘情况：localSummary 以 summary 开头 |
| 388-389 | `return None` | ValueError 解析 PROFILE_LLM_TIMEOUT_SECONDS |
| 403-405 | 异常捕获 → 本地降级 | OpenAI 客户端初始化失败降级 |
| 543 | `continue` | kpId 在 directCounts 中但不在 masteryByKp 中 |
| 666 | `raise ValueError(...)` | 不支持的学习情况来源名（已由 schemas 校验排除） |
| 995 | `return directTrend.value, False` | 等值趋势冲突且值相同（边缘路径） |

**评估**：全部 12 行都是合理的未覆盖代码 — 要么需要外部依赖（OpenAI SDK），要么是防御性边缘路径。

---

## 总结

- ✅ 10 种事件类型的 data 子模型与通知清单完全一致
- ✅ 字段约束（strict bool、Identifier 正则、时区强制、null 拒绝）全部到位
- ✅ 三态响应联合判别完整
- ✅ 8 种拒绝条件均有对应校验
- ✅ 过渡兼容模型已清除
- ✅ 路由与响应模型一致
- ✅ 221 项测试全部通过，schemas.py 覆盖率 99%
- ℹ️ 3 行未覆盖代码均为错误路径中的 raise 语句
